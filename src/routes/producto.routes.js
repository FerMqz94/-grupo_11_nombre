const express = require("express");
const router = express.Router();

// const { productDetail, List} = require("../controllers/products");

// router.get("/detalle/:id", productDetail);
// router.get("/lista", List); 
module.exports = router;